import styled from 'styled-components'

import 'react-flexbox-grid/dist/react-flexbox-grid'
import { Grid as FlexGrid } from 'react-flexbox-grid'

export const Grid = styled(FlexGrid)`
  padding: 0 32px;
`